

const { gets, print } = require("./exportação de funções auxiliares");

//  console.log(funcoes.gets());

/*const pessoa = {
  nome: "gabriel"
};

const { nome } = pessoa; //destruction */

print(gets());
